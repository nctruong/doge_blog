<h2>Blog Time!</h2>

<h4>Current Posts</h4>
<ul>
  <% @posts.each do |post| %>
  <li><%= link_to post.title, post_path(post) %> <%= post.created_at %> </li>
  <% end %>
</ul>

<p> <%= link_to 'New Post', new_post_path %> </p>
