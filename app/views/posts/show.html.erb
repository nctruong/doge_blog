<h2>Post Title: <%= @post.title %></h2>
<h4>Posted On: <%= @post.created_at  %></h4>

<p><%= @post.post_text %> </p>

<% if user_signed_in? && current_user.id == @post.user_id %>
  <%= link_to 'Edit', edit_post_path(@post), :class => 'btn btn-primary' %>
  <%= link_to 'Delete', post_path(@post),
                      :data => {:confirm => 'Are you sure?',
                      :method => 'delete' }, :class => 'btn btn-danger' %>
  <%= link_to 'Add Comment', new_post_comment_path(@post), :class => 'btn btn-primary', id: "new_comment_link", remote: true %>
<% elsif user_signed_in? %>
  <%= link_to 'Add Comment', new_post_comment_path(@post), :class => 'btn btn-primary', id: "new_comment_link", remote: true %>
<% end %>

<% if @post.comments.any? %>
  <h6>Comments for: <%= @post.title %> </h6>
  <ul id="current_comments">
    <%= render @post.comments %>
  </ul>
<% elsif user_signed_in? %>
  <hr>
  <h6>Be The First To Comment!</h6>
<% end %>

<hr>

<%= link_to 'Back', posts_path, :class => 'btn btn-primary' %>
